# Public: browser GET requests use the backend directly
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Server-only: used by Next.js write Route Handlers
# Never rename API_KEY with a NEXT_PUBLIC_ prefix.
API_KEY=
BACKEND_INTERNAL_URL=http://localhost:8000
